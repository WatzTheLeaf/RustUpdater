# How to use

```
.\patcher_cli.exe --product "MyProgram" --new-dir "C:\Program\Dist" --version "1.0.0" --exe "Program.exe" --output "C:\output"
```

Do the same with update, just change the version parameter.